# Third-Party Notices

The repository contains no LuaSocket binaries. Users install LuaSocket
separately under its own license.

`reaper/json.lua` is a small project-owned JSON codec carried forward from the
initial Metal MIDI Generator prototype and released here under this
repository's MIT license.

